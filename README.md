# my-backend-nest

Minimal NestJS backend scaffold created for CI testing.

Usage

- Install dependencies: `npm install`
- Run in development: `npm run start:dev`
- Run tests: `npm run test`

This repo contains a basic NestJS app plus CI workflow for testing.
